# Руководство по участию в проекте FamOS

Добро пожаловать в команду разработки FamOS! Этот документ поможет вам начать работу над проектом.

## Начало работы

### 1. Клонирование репозитория

```bash
git clone https://github.com/Yerassyl-hub/FamOS.git
cd FamOS
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка окружения

Создайте файл `.env` в корне проекта (если нужен):

```env
MONGODB_URI=mongodb://localhost:27017/famos
REDIS_HOST=localhost
REDIS_PORT=6380
PORT=3000
```

### 4. Запуск сервисов

```bash
# Запустить MongoDB и Redis
docker-compose up -d

# Запустить приложение
npm run start:dev
```

## Рабочий процесс

### Ветвление

1. **Создайте ветку от `main`**:
   ```bash
   git checkout -b feature/название-функции
   # или
   git checkout -b fix/описание-бага
   ```

2. **Названия веток**:
   - `feature/` - новая функциональность
   - `fix/` - исправление багов
   - `refactor/` - рефакторинг кода
   - `docs/` - обновление документации
   - `test/` - добавление тестов

### Коммиты

Используйте понятные сообщения коммитов:

```bash
git commit -m "feat: добавить синхронизацию чатов"
git commit -m "fix: исправить ошибку в обработке сообщений"
git commit -m "docs: обновить README"
```

**Префиксы:**
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - обновление зависимостей, конфигурации

### Pull Request

1. **Перед созданием PR**:
   - Убедитесь, что код компилируется: `npm run build`
   - Проверьте линтер: `npm run lint`
   - Обновите документацию, если нужно

2. **Создайте Pull Request**:
   - Опишите, что было сделано
   - Укажите связанные issues (если есть)
   - Попросите ревью у команды

3. **Шаблон описания PR**:
   ```markdown
   ## Описание
   Краткое описание изменений

   ## Тип изменений
   - [ ] Новая функциональность
   - [ ] Исправление бага
   - [ ] Рефакторинг
   - [ ] Документация

   ## Чеклист
   - [ ] Код протестирован
   - [ ] Документация обновлена
   - [ ] Нет ошибок линтера
   ```

## Структура проекта

```
FamOS/
├── server/src/
│   ├── api/              # REST API контроллеры
│   ├── chat/             # Управление чатами
│   ├── chat-sync/        # Синхронизация чатов
│   ├── daily-report/     # Ежедневные отчеты
│   ├── message/         # Управление сообщениями
│   ├── message-processor/ # Обработка сообщений
│   ├── queue/           # Конфигурация Redis
│   ├── user-style/       # Анализ стиля общения
│   ├── ai-response/     # Генерация ответов
│   └── wa/              # WhatsApp интеграция
├── docker-compose.yml
└── package.json
```

## Стандарты кода

### TypeScript

- Используйте TypeScript строго
- Избегайте `any`, используйте конкретные типы
- Комментируйте сложную логику

### NestJS

- Следуйте архитектуре NestJS (модули, сервисы, контроллеры)
- Используйте декораторы правильно
- Инжектируйте зависимости через конструктор

### Именование

- Файлы: `kebab-case` (например: `user-style.service.ts`)
- Классы: `PascalCase` (например: `UserStyleService`)
- Переменные/функции: `camelCase` (например: `getUserStyle`)

## Тестирование

```bash
# Запустить тесты
npm test

# Запустить тесты с покрытием
npm run test:cov
```

## Вопросы и помощь

- Создайте Issue для обсуждения
- Используйте Discussions для вопросов
- Обращайтесь к команде в чате проекта

## Полезные команды

```bash
# Разработка
npm run start:dev      # Запуск в режиме разработки
npm run build          # Сборка проекта
npm run start:prod     # Запуск в продакшене

# Качество кода
npm run lint           # Проверка линтером
npm run format         # Форматирование кода

# Docker
docker-compose up -d    # Запуск сервисов
docker-compose down     # Остановка сервисов
docker-compose logs -f  # Просмотр логов
```

## Безопасность

- **НЕ коммитьте**:
  - `.env` файлы
  - `wa_auth_info/` (данные авторизации WhatsApp)
  - `mongo_data/` и `redis_data/`
  - API ключи и пароли

- Все секреты должны быть в переменных окружения

## Спасибо за вклад! 🚀
