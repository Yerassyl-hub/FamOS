# ✅ Чеклист для деплоя на Render.com

## 📋 Перед деплоем

- [x] Репозиторий в GitHub: `Yerassyl-hub/FamOS`
- [x] `render.yaml` создан
- [x] `server/package.json` создан
- [x] `main.ts` слушает на `0.0.0.0`
- [x] Build команда работает локально
- [x] Документация создана

## 🚀 Шаги деплоя

### 1. MongoDB
- [ ] Создать MongoDB сервис на Render
- [ ] Имя: `famos-mongo`
- [ ] Скопировать Internal Connection String
- [ ] Добавить в переменные окружения как `MONGODB_URI`

### 2. Redis
- [ ] Создать Redis сервис на Render
- [ ] Имя: `famos-redis`
- [ ] Скопировать Internal Redis URL
- [ ] Добавить в переменные окружения как `REDIS_HOST` и `REDIS_PORT`

### 3. Web Service
- [ ] Подключить GitHub репозиторий
- [ ] Установить **Root Directory: `server`** ⚠️ ВАЖНО!
- [ ] Build Command: `npm install && npm run build`
- [ ] Start Command: `npm run start:prod`
- [ ] Добавить переменные окружения (см. ниже)

### 4. Переменные окружения

```env
NODE_ENV=production
PORT=10000
MONGODB_URI=mongodb://famos-mongo:27017/famos
REDIS_HOST=famos-redis
REDIS_PORT=6379
FRONTEND_URL=*
```

### 5. Проверка после деплоя

- [ ] `https://your-app.onrender.com/api/health` возвращает `{"status":"ok"}`
- [ ] `https://your-app.onrender.com/api/stats` возвращает статистику
- [ ] Логи не показывают ошибок подключения к MongoDB/Redis

## ⚠️ Важные моменты

1. **Root Directory** должен быть `server` (не корень!)
2. **MongoDB/Redis** - используйте Internal Connection для бесплатного плана
3. **Free план** - сервисы "засыпают" после 15 минут бездействия
4. **WhatsApp** - может не работать на Render (нужен постоянный процесс)

## 📚 Документация

- `RENDER_DEPLOY.md` - подробная инструкция
- `RENDER_QUICK_START.md` - быстрый старт
- `render.yaml` - конфигурация для автоматического деплоя
