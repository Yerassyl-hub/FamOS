# 🔧 Решение проблем с 404 ошибками

## ✅ Все endpoints реализованы!

Все требуемые endpoints **уже реализованы** в бэкенде:
- ✅ `GET /api/health` - строка 23
- ✅ `GET /api/status` - строка 28
- ✅ `GET /api/whatsapp/status` - строка 140
- ✅ `POST /api/whatsapp/generate-qr` - строка 186
- ✅ `POST /api/whatsapp/disconnect` - строка 165
- ✅ Все остальные endpoints

## 🔍 Диагностика проблемы

### Шаг 1: Проверьте, что бэкенд запущен

```bash
# Проверка через curl
curl http://localhost:3000/api/health

# Должен вернуть:
# {"status":"ok","timestamp":"..."}
```

### Шаг 2: Проверьте URL во фронтенде

**❌ Неправильно:**
```javascript
fetch('/health')  // Без /api
fetch('http://localhost:3000/health')  // Без /api
```

**✅ Правильно:**
```javascript
fetch('http://localhost:3000/api/health')
// или
const API_URL = 'http://localhost:3000/api';
fetch(`${API_URL}/health`)
```

### Шаг 3: Перезапустите бэкенд

После изменений **обязательно перезапустите**:

```bash
# Остановите (Ctrl+C)
# Запустите заново:
npm run start:dev
```

### Шаг 4: Проверьте консоль бэкенда

При запуске должны быть логи:
```
Application is running on: http://localhost:3000
API available at: http://localhost:3000/api
Available endpoints:
  GET  /api/health
  GET  /api/status
  ...
```

## 🛠️ Решение

### Если все еще 404:

1. **Убедитесь, что используете правильный URL:**
   ```typescript
   // В API client
   const API_URL = 'http://localhost:3000/api';  // ✅ С /api
   ```

2. **Проверьте, что бэкенд запущен:**
   ```bash
   # Должен вернуть данные
   curl http://localhost:3000/api/health
   ```

3. **Проверьте CORS в консоли браузера:**
   - Откройте DevTools → Network
   - Посмотрите на запрос
   - Проверьте заголовки ответа

4. **Перезапустите бэкенд:**
   ```bash
   # Остановите текущий процесс
   # Запустите заново
   npm run start:dev
   ```

## 📋 Чеклист

- [ ] Бэкенд запущен на порту 3000
- [ ] Используется правильный URL: `http://localhost:3000/api/...`
- [ ] Бэкенд перезапущен после изменений
- [ ] `curl http://localhost:3000/api/health` возвращает данные
- [ ] В консоли бэкенда видны логи о запуске

## 🎯 Быстрая проверка

```bash
# 1. Проверка health
curl http://localhost:3000/api/health

# 2. Проверка stats
curl http://localhost:3000/api/stats

# 3. Проверка whatsapp status
curl http://localhost:3000/api/whatsapp/status
```

Если все три команды возвращают данные - бэкенд работает правильно! ✅

## ⚠️ Важно

**НЕ добавляйте двойной префикс `/api`:**

```typescript
// ❌ Неправильно
const API_URL = 'http://localhost:3000/api';
fetch(`${API_URL}/api/health`)  // Получится /api/api/health

// ✅ Правильно
const API_URL = 'http://localhost:3000/api';
fetch(`${API_URL}/health`)  // Получится /api/health
```

## 📞 Если проблема осталась

1. Проверьте логи бэкенда на ошибки
2. Убедитесь, что порт 3000 не занят другим приложением
3. Проверьте, что все зависимости установлены: `npm install`
4. Пересоберите проект: `npm run build`
