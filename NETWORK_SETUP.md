# 🌐 Настройка для работы в локальной сети

## 📋 Сценарий

- **Бэкенд:** работает на вашем ноутбуке
- **Фронтенд:** работает на другом ноутбуке
- **Сеть:** оба ноутбука в одной локальной сети (Wi-Fi/LAN)

## ✅ Что уже настроено

- ✅ Бэкенд слушает на `0.0.0.0` (доступен по сети)
- ✅ CORS настроен для работы с любым origin в development

## 🔧 Шаг 1: Узнайте IP адрес вашего ноутбука

### Windows (PowerShell):

```powershell
# Вариант 1: Через ipconfig
ipconfig | Select-String "IPv4"

# Вариант 2: Через Get-NetIPAddress
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -notlike "*Loopback*"} | Select-Object IPAddress, InterfaceAlias

# Вариант 3: Простой способ
ipconfig | findstr /i "IPv4"
```

**Пример результата:**
```
IPv4 Address. . . . . . . . . . . . : 192.168.1.100
```

**Ваш IP адрес:** `192.168.1.100` (замените на свой)

### Windows (CMD):

```cmd
ipconfig
```

Найдите строку "IPv4 Address" (не "127.0.0.1")

## 🔧 Шаг 2: Проверьте настройки бэкенда

### CORS уже настроен правильно!

В `server/src/main.ts` уже есть:

```typescript
app.enableCors({
  origin: function (origin, callback) {
    // В development разрешает все origins
    if (process.env.NODE_ENV !== 'production') {
      return callback(null, true);
    }
    // ...
  },
  // ...
});
```

**Это означает:** В development режиме бэкенд принимает запросы с любого IP адреса! ✅

## 🚀 Шаг 3: Запустите бэкенд

```powershell
# Убедитесь, что используете development режим
npm run start:dev
```

Бэкенд будет доступен на:
- `http://localhost:3000/api` (с вашего ноутбука)
- `http://192.168.1.100:3000/api` (с другого ноутбука) ⚠️ Замените на ваш IP!

## 🔧 Шаг 4: Настройте фронтенд на другом ноутбуке

### Создайте `.env.local`:

```env
# Замените 192.168.1.100 на IP адрес вашего ноутбука!
NEXT_PUBLIC_API_URL=http://192.168.1.100:3000/api
```

### Или в коде:

```typescript
// lib/api.ts
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://192.168.1.100:3000/api';
```

## 🔥 Шаг 5: Проверьте файрвол

### Windows Firewall:

1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Дополнительные параметры"
3. Правила для входящих подключений → Создать правило
4. Тип: Для порта
5. Протокол: TCP
6. Порт: 3000
7. Действие: Разрешить подключение
8. Применить для всех профилей

### Или через PowerShell (от имени администратора):

```powershell
New-NetFirewallRule -DisplayName "FamOS Backend" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

## ✅ Проверка работы

### С вашего ноутбука:

```bash
curl http://localhost:3000/api/health
```

### С другого ноутбука:

```bash
# Замените IP на ваш!
curl http://192.168.1.100:3000/api/health
```

**Должен вернуть:**
```json
{"status":"ok","timestamp":"..."}
```

## 🐛 Troubleshooting

### Проблема: "Connection refused"

**Причина:** Файрвол блокирует порт 3000

**Решение:**
1. Откройте порт 3000 в файрволе (см. выше)
2. Или временно отключите файрвол для теста

### Проблема: "CORS error"

**Причина:** CORS не настроен правильно

**Решение:** Убедитесь, что используете `npm run start:dev` (development режим)

### Проблема: "Cannot connect"

**Причина:** Неправильный IP адрес

**Решение:**
1. Проверьте IP адрес: `ipconfig`
2. Убедитесь, что оба ноутбука в одной сети
3. Попробуйте пинг: `ping 192.168.1.100`

### Проблема: IP адрес меняется

**Причина:** DHCP выдает новый IP при переподключении

**Решение:**
1. Зафиксируйте IP в настройках сети (статический IP)
2. Или используйте имя компьютера (если настроен DNS)

## 📝 Быстрая инструкция для фронтенда

На другом ноутбуке создайте файл `.env.local`:

```env
# Замените на IP адрес ноутбука с бэкендом!
NEXT_PUBLIC_API_URL=http://192.168.1.100:3000/api
```

И используйте в коде:

```typescript
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000/api';
```

## 🎯 Итог

1. ✅ Узнайте IP адрес: `ipconfig`
2. ✅ Запустите бэкенд: `npm run start:dev`
3. ✅ Откройте порт 3000 в файрволе
4. ✅ На другом ноутбуке используйте: `http://ВАШ_IP:3000/api`

## 💡 Совет

Если IP адрес часто меняется, можно:
1. Зафиксировать IP в настройках сети
2. Использовать имя компьютера (если настроен DNS)
3. Или просто обновлять `.env.local` при изменении IP
