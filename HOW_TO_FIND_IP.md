# 🔍 Как узнать IP адрес вашего ноутбука

## ✅ Способ 1: PowerShell (Рекомендуется)

### Шаг 1: Откройте PowerShell

**Вариант A: Через поиск Windows**
1. Нажмите `Win + S` (или кнопку поиска)
2. Введите: `PowerShell`
3. Нажмите Enter

**Вариант B: Через меню Пуск**
1. Откройте меню Пуск
2. Найдите "Windows PowerShell"
3. Кликните на него

**Вариант C: Через CMD**
1. Нажмите `Win + R`
2. Введите: `powershell`
3. Нажмите Enter

### Шаг 2: Выполните команду

Скопируйте и вставьте эту команду в PowerShell:

```powershell
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -notlike "*Loopback*"} | Select-Object IPAddress, InterfaceAlias
```

**Или более простая команда:**

```powershell
ipconfig | findstr /i "IPv4"
```

### Шаг 3: Найдите ваш IP

Вы увидите что-то вроде:
```
IPAddress      InterfaceAlias
---------      --------------
192.168.1.100  Wi-Fi
```

**Ваш IP адрес:** `192.168.1.100` (замените на свой!)

---

## ✅ Способ 2: Через CMD (Command Prompt)

### Шаг 1: Откройте CMD

1. Нажмите `Win + R`
2. Введите: `cmd`
3. Нажмите Enter

### Шаг 2: Выполните команду

```cmd
ipconfig
```

### Шаг 3: Найдите IPv4 Address

Прокрутите вниз и найдите строку:
```
IPv4 Address. . . . . . . . . . . . : 192.168.1.100
```

**Ваш IP адрес:** `192.168.1.100`

---

## ✅ Способ 3: Через настройки Windows

1. Откройте **Настройки** (Win + I)
2. Перейдите в **Сеть и Интернет**
3. Выберите ваше подключение (Wi-Fi или Ethernet)
4. Прокрутите вниз до **Свойства**
5. Найдите **IPv4-адрес**

---

## 🎯 Что делать дальше?

После того, как узнали IP адрес (например, `192.168.1.100`):

1. **На вашем ноутбуке (бэкенд):**
   - Запустите бэкенд: `npm run start:dev`
   - Бэкенд будет доступен на: `http://192.168.1.100:3000/api`

2. **На другом ноутбуке (фронтенд):**
   - Создайте `.env.local`:
   ```env
   NEXT_PUBLIC_API_URL=http://192.168.1.100:3000/api
   ```
   - Замените `192.168.1.100` на ваш реальный IP!

---

## ⚠️ Важно

- **IP адрес может меняться** при переподключении к Wi-Fi
- Если IP изменился, обновите `.env.local` на другом ноутбуке
- Убедитесь, что оба ноутбука в **одной Wi-Fi сети**

---

## 🐛 Если не видите IP адрес

### Проблема: "Команда не найдена"

**Решение:** Убедитесь, что используете PowerShell, а не CMD

### Проблема: Показывает только 127.0.0.1

**Решение:** 
1. Убедитесь, что подключены к Wi-Fi
2. Попробуйте другую команду:
   ```powershell
   ipconfig | findstr /i "IPv4"
   ```

### Проблема: Несколько IP адресов

**Решение:** Используйте IP адрес, который начинается с `192.168.` или `10.` (это локальный IP)

---

## 📝 Быстрая команда

Самый простой способ - откройте PowerShell и выполните:

```powershell
ipconfig | findstr /i "IPv4"
```

Скопируйте IP адрес (например, `192.168.1.100`) и используйте его в `.env.local` на другом ноутбуке!
