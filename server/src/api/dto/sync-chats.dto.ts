// DTO для синхронизации чатов (пока пустой, но можно добавить параметры)
export class SyncChatsDto {
  // Можно добавить параметры в будущем, например:
  // @IsOptional()
  // @IsBoolean()
  // force?: boolean;
}
