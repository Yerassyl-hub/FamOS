# 📱 Настройка фронтенда для работы с бэкендом в сети

## 🎯 Сценарий

- **Бэкенд:** работает на ноутбуке `192.168.1.100:3000`
- **Фронтенд:** работает на другом ноутбуке
- **Нужно:** подключить фронтенд к бэкенду по сети

## ✅ Шаг 1: Узнайте IP адрес ноутбука с бэкендом

Попросите владельца ноутбука с бэкендом выполнить:

```powershell
ipconfig | findstr /i "IPv4"
```

Или:
```powershell
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -notlike "*Loopback*"}
```

**Пример IP:** `192.168.1.100`

## ✅ Шаг 2: Создайте `.env.local`

В корне проекта фронтенда создайте файл `.env.local`:

```env
# Замените 192.168.1.100 на реальный IP адрес ноутбука с бэкендом!
NEXT_PUBLIC_API_URL=http://192.168.1.100:3000/api
```

## ✅ Шаг 3: Настройте API клиент

### Вариант 1: Использовать переменную окружения

```typescript
// lib/api.ts или utils/api.ts
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000/api';

export const apiClient = axios.create({
  baseURL: API_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});
```

### Вариант 2: Прямое указание IP

```typescript
// lib/api.ts
const API_URL = 'http://192.168.1.100:3000/api'; // Замените на ваш IP!

export const apiClient = axios.create({
  baseURL: API_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});
```

## ✅ Шаг 4: Перезапустите фронтенд

После изменения `.env.local`:

```bash
# Остановите фронтенд (Ctrl+C)
# Запустите заново
npm run dev
```

## ✅ Шаг 5: Проверьте подключение

### Тест 1: Health endpoint

```bash
curl http://192.168.1.100:3000/api/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"..."}
```

### Тест 2: Из браузера

Откройте в браузере:
```
http://192.168.1.100:3000/api/health
```

Должен вернуть JSON.

## 🐛 Troubleshooting

### Проблема: "Network Error" или "Connection refused"

**Причины:**
1. Бэкенд не запущен
2. Неправильный IP адрес
3. Файрвол блокирует порт 3000
4. Ноутбуки в разных сетях

**Решение:**
1. Проверьте, что бэкенд запущен: `curl http://192.168.1.100:3000/api/health`
2. Проверьте IP адрес: `ipconfig`
3. Попросите открыть порт 3000 в файрволе
4. Убедитесь, что оба ноутбука в одной Wi-Fi сети

### Проблема: "CORS error"

**Причина:** CORS не настроен

**Решение:** Убедитесь, что бэкенд запущен в development режиме:
```bash
npm run start:dev
```

### Проблема: "404 Not Found"

**Причина:** Неправильный URL

**Решение:** Убедитесь, что используете `/api` в URL:
```typescript
// ✅ Правильно
const API_URL = 'http://192.168.1.100:3000/api';

// ❌ Неправильно
const API_URL = 'http://192.168.1.100:3000';
```

## 📝 Пример использования

```typescript
// hooks/useApi.ts
import { useQuery } from '@tanstack/react-query';
import { apiClient } from '@/lib/api';

export function useStats() {
  return useQuery({
    queryKey: ['stats'],
    queryFn: async () => {
      const { data } = await apiClient.get('/stats');
      return data;
    },
  });
}

// components/Dashboard.tsx
import { useStats } from '@/hooks/useApi';

export function Dashboard() {
  const { data, isLoading } = useStats();
  
  if (isLoading) return <div>Loading...</div>;
  
  return (
    <div>
      <h1>Total Messages: {data?.totalMessages}</h1>
      <h2>Total Chats: {data?.totalChats}</h2>
    </div>
  );
}
```

## 🎯 Итог

1. ✅ Узнайте IP адрес ноутбука с бэкендом
2. ✅ Создайте `.env.local` с правильным IP
3. ✅ Используйте `process.env.NEXT_PUBLIC_API_URL` в коде
4. ✅ Перезапустите фронтенд
5. ✅ Проверьте подключение

## 💡 Важно

- **IP адрес может меняться** при переподключении к Wi-Fi
- Если IP изменился, обновите `.env.local`
- Для постоянного IP зафиксируйте его в настройках сети
