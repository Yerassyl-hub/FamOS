# 🚀 Деплой бэкенда на Render.com

## Шаг 1: Подготовка

### 1. Создайте аккаунт на Render
- Перейдите на https://render.com
- Зарегистрируйтесь или войдите

### 2. Подключите GitHub репозиторий
- В Render Dashboard → New → Web Service
- Подключите репозиторий: `Yerassyl-hub/FamOS`

## Шаг 2: Настройка сервиса

### Основные настройки:
- **Name:** `famos-backend`
- **Environment:** `Node`
- **Region:** Выберите ближайший
- **Branch:** `main`
- **Root Directory:** (оставьте пустым, проект в корне)

### Build & Deploy:
- **Build Command:** `npm install && npm run build`
- **Start Command:** `npm run start:prod`

## Шаг 3: Переменные окружения

Добавьте в Render Dashboard → Environment:

```env
NODE_ENV=production
PORT=10000
MONGODB_URI=mongodb+srv://... (MongoDB Atlas или другой)
REDIS_HOST=your-redis-host.render.com
REDIS_PORT=6379
FRONTEND_URL=https://your-frontend.onrender.com
```

## Шаг 4: MongoDB на Render

### Вариант 1: MongoDB Atlas (рекомендуется)
1. Создайте аккаунт на https://www.mongodb.com/cloud/atlas
2. Создайте кластер (бесплатный M0)
3. Получите connection string
4. Добавьте в переменные окружения как `MONGODB_URI`

### Вариант 2: Render MongoDB
1. Render Dashboard → New → MongoDB
2. Создайте MongoDB сервис
3. Используйте Internal Database URL

## Шаг 5: Redis на Render

1. Render Dashboard → New → Redis
2. Создайте Redis сервис
3. Используйте Internal Redis URL
4. Добавьте в переменные окружения

## Шаг 6: Деплой

1. Нажмите "Create Web Service"
2. Render автоматически:
   - Клонирует репозиторий
   - Установит зависимости
   - Соберет проект
   - Запустит сервис

## Шаг 7: Проверка

После деплоя проверьте:
```bash
curl https://your-app.onrender.com/api/health
```

## ⚠️ Важные замечания

### 1. Root Directory
Убедитесь, что указан `server` как Root Directory, т.к. код находится в папке `server/`

### 2. Порт
Render использует переменную `PORT` или порт из окружения. По умолчанию это `10000`.

### 3. MongoDB и Redis
На Render нужно использовать внешние сервисы:
- MongoDB Atlas (бесплатный)
- Render Redis (бесплатный)

### 4. WhatsApp сессия
Файлы `wa_auth_info/` не сохранятся между деплоями. Нужно будет заново сканировать QR код.

### 5. Логи
Логи доступны в Render Dashboard → Logs

## 📋 Чеклист деплоя

- [ ] Создан аккаунт на Render
- [ ] Подключен GitHub репозиторий
- [ ] Указан Root Directory: `server`
- [ ] Настроены Build и Start команды
- [ ] Добавлены переменные окружения
- [ ] Создан MongoDB (Atlas или Render)
- [ ] Создан Redis (Render)
- [ ] Сервис успешно задеплоен
- [ ] Проверен health endpoint

## 🔗 Полезные ссылки

- Render Dashboard: https://dashboard.render.com
- MongoDB Atlas: https://www.mongodb.com/cloud/atlas
- Документация Render: https://render.com/docs

## 🎯 После деплоя

1. Обновите API URL во фронтенде:
   ```env
   NEXT_PUBLIC_API_URL=https://your-app.onrender.com/api
   ```

2. Проверьте CORS - убедитесь, что `FRONTEND_URL` указан правильно

3. Подключите WhatsApp заново (QR код будет в логах Render)
