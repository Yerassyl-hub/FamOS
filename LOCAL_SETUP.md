# 💻 Локальный запуск FamOS

## ✅ Что нужно для локального запуска

### Обязательно:
1. **Node.js** (v18 или выше)
2. **Docker Desktop** (для MongoDB и Redis)
3. **npm** (устанавливается с Node.js)

### НЕ нужно:
- ❌ Render.com
- ❌ Облачные сервисы
- ❌ Интернет (кроме установки пакетов)

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
npm install
```

### 2. Запустите Docker сервисы (MongoDB и Redis)

```bash
docker-compose up -d
```

Или используйте скрипт:
```powershell
.\start-all.ps1
```

### 3. Запустите бэкенд

```bash
npm run start:dev
```

Или используйте скрипт:
```powershell
.\start-all.ps1
```

## ✅ Готово!

Бэкенд будет доступен на:
```
http://localhost:3000/api
```

## 📋 Проверка работы

### 1. Проверьте Docker сервисы

```bash
docker ps
```

Должны быть запущены:
- `famos-mongo` (порт 27017)
- `famos-redis` (порт 6380)

### 2. Проверьте бэкенд

```bash
curl http://localhost:3000/api/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"..."}
```

### 3. Проверьте статистику

```bash
curl http://localhost:3000/api/stats
```

## 🔧 Настройка

### Переменные окружения (опционально)

Создайте `.env` файл в корне проекта:

```env
# MongoDB (по умолчанию используется localhost:27017)
MONGODB_URI=mongodb://localhost:27017/famos

# Redis (по умолчанию используется localhost:6380)
REDIS_HOST=localhost
REDIS_PORT=6380

# Порт бэкенда (по умолчанию 3000)
PORT=3000

# CORS (для фронтенда)
FRONTEND_URL=http://localhost:3001
```

**Примечание:** Если не создаете `.env`, бэкенд использует значения по умолчанию.

## 📁 Структура проекта

```
FamOS/
├── server/              # Код бэкенда
│   └── src/
├── docker-compose.yml   # Docker конфигурация
├── package.json         # Зависимости
└── .env                 # Переменные окружения (опционально)
```

## 🛠️ Полезные команды

### Запуск всех сервисов
```powershell
.\start-all.ps1
```

### Мониторинг
```powershell
.\monitor.ps1
```

### Остановка Docker
```bash
docker-compose down
```

### Перезапуск бэкенда
```bash
# Остановите (Ctrl+C)
# Запустите заново:
npm run start:dev
```

## ⚠️ Частые проблемы

### Проблема: "Port already in use"
**Решение:** Измените порт в `.env` или остановите другой процесс на порту 3000

### Проблема: "Cannot connect to MongoDB"
**Решение:** Убедитесь, что Docker запущен: `docker ps`

### Проблема: "Cannot connect to Redis"
**Решение:** Проверьте, что Redis контейнер запущен: `docker ps`

## 🎯 Итог

**Для локального запуска НЕ нужен Render!**

Нужно только:
1. ✅ Node.js
2. ✅ Docker
3. ✅ `npm install`
4. ✅ `docker-compose up -d`
5. ✅ `npm run start:dev`

**Render нужен только для:**
- Деплоя в интернет (продакшен)
- Доступа к бэкенду извне
- Постоянного онлайн (для WhatsApp бота)
